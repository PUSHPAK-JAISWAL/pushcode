spring:
  application:
    name: backend

execution:
  max-time-seconds: 10
  idle-timeout-seconds: 15
  memory-limit: 256m
  cpu-limit: 0.5

docker:
  images:
    python: pushcode-python
    c: pushcode-c
    cpp: pushcode-cpp
    java: pushcode-java